import 'package:flutter/material.dart';

class CategoryNoItem extends StatelessWidget {
  const CategoryNoItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('No categories available'));
  }
}
