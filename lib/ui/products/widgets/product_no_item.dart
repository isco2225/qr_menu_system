import 'package:flutter/material.dart';

class ProductNoItem extends StatelessWidget {
  const ProductNoItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('No products found'));
  }
}
